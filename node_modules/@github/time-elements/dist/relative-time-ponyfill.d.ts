export declare class RelativeTimeFormat implements Intl.RelativeTimeFormat {
    formatToParts(): never[];
    resolvedOptions(): {
        readonly locale: "en";
        readonly style: "long";
        readonly numeric: "auto";
        readonly numberingSystem: "nu";
    };
    format(value: number, unit: string): string;
}
